import { BadRequestFilter } from './bad-request-filter.filter';

describe('BadRequestFilterFilter', () => {
  it('should be defined', () => {
    expect(new BadRequestFilter()).toBeTruthy();
  });
});
